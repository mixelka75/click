# Employer handlers package initializer
# Keep minimal to prevent circular imports.
# Import submodules directly in bot/main.py, e.g.:
#   import bot.handlers.employer.vacancy_handlers as vacancy_handlers

__all__: list[str] = []
