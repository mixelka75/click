# Инструкция по тестированию публикации в Telegram каналы

## Быстрая настройка для тестирования

### Шаг 1: Создайте тестовый канал

1. Откройте Telegram на телефоне или в десктопном приложении
2. Нажмите на меню (три полоски) → New Channel
3. Введите название канала (например: "CLICK Test Channel")
4. Выберите тип канала:
   - **Public** (проще для тестирования) - придумайте username, например `@click_test_123`
   - **Private** - понадобится получить chat_id

5. Нажмите Create Channel

### Шаг 2: Добавьте бота как администратора

1. Откройте только что созданный канал
2. Нажмите на название канала сверху
3. Нажмите на "Administrators" или "Администраторы"
4. Нажмите "Add Administrator" или "Добавить администратора"
5. Найдите вашего бота по username (который вы создали у @BotFather)
6. **Важно:** Дайте боту права на публикацию сообщений:
   - ✅ Post Messages (Публиковать сообщения)
   - ✅ Edit Messages (Редактировать сообщения) - опционально
   - ✅ Delete Messages (Удалять сообщения) - опционально

### Шаг 3: Настройте .env файл

Откройте файл `.env` в корне проекта и замените каналы на ваш тестовый:

```bash
# Для быстрого тестирования - все публикации идут в один канал
CHANNEL_VACANCIES_BARMEN=@click_test_123
CHANNEL_VACANCIES_WAITERS=@click_test_123
CHANNEL_VACANCIES_COOKS=@click_test_123
CHANNEL_VACANCIES_BARISTA=@click_test_123
CHANNEL_VACANCIES_ADMIN=@click_test_123
CHANNEL_VACANCIES_SUPPORT=@click_test_123
CHANNEL_VACANCIES_OTHER=@click_test_123
CHANNEL_VACANCIES_GENERAL=@click_test_123

CHANNEL_RESUMES_BARMEN=@click_test_123
CHANNEL_RESUMES_WAITERS=@click_test_123
CHANNEL_RESUMES_COOKS=@click_test_123
CHANNEL_RESUMES_BARISTA=@click_test_123
CHANNEL_RESUMES_ADMIN=@click_test_123
CHANNEL_RESUMES_SUPPORT=@click_test_123
CHANNEL_RESUMES_OTHER=@click_test_123
CHANNEL_RESUMES_GENERAL=@click_test_123
```

**Замените `@click_test_123` на username вашего канала!**

### Шаг 4: Перезапустите приложение

```bash
# Если используете Docker
docker-compose down
docker-compose up -d

# Или если локально
# Остановите процессы и запустите заново
python -m backend.main
python -m bot.main
```

### Шаг 5: Протестируйте публикацию

#### Тест 1: Создание и публикация вакансии

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Выберите "Я работодатель"
4. Нажмите "📝 Создать вакансию"
5. Заполните все поля (можно использовать тестовые данные)
6. В конце нажмите "Опубликовать"
7. **Проверьте:** В вашем тестовом канале должен появиться пост с вакансией

#### Тест 2: Создание и публикация резюме

1. Создайте нового пользователя или смените роль
2. Выберите "Я соискатель"
3. Нажмите "📝 Создать резюме"
4. Заполните все поля
5. В конце нажмите "Опубликовать"
6. **Проверьте:** В вашем тестовом канале должен появиться пост с резюме

## Решение проблем

### Публикация не работает - бот ничего не постит в канал

**Проверьте:**

1. ✅ Бот добавлен в канал как администратор?
2. ✅ У бота есть права на публикацию сообщений?
3. ✅ Username канала правильно указан в .env? (с символом @)
4. ✅ Приложение перезапущено после изменения .env?

**Как проверить права бота:**
1. Откройте канал
2. Информация о канале → Administrators
3. Найдите вашего бота в списке
4. Нажмите на бота → проверьте галочки

### Бот пишет "Failed to publish"

**Проверьте логи:**

```bash
# Для Docker
docker-compose logs backend | grep "Failed to publish"
docker-compose logs bot | grep "Failed to publish"

# Для локального запуска
# Смотрите в консоль или logs/app.log
cat logs/app.log | grep "Failed to publish"
```

**Типичные ошибки:**

1. **"Chat not found"** - неправильный username канала или бот не добавлен
2. **"Need administrator rights"** - бот не является администратором
3. **"Have no rights to send messages"** - нет прав на публикацию

### Private канал (получение chat_id)

Если используете Private канал:

1. Создайте канал как Private
2. Добавьте бота @getmyid_bot в канал
3. Форвардните любое сообщение из канала боту @getmyid_bot
4. Он вернет chat_id (например: `-1001234567890`)
5. Используйте этот ID в .env:

```env
CHANNEL_VACANCIES_GENERAL=-1001234567890
```

## Тестирование разных каналов для разных категорий

Если хотите протестировать публикацию в разные каналы:

1. Создайте несколько тестовых каналов:
   - @test_barmen
   - @test_waiters
   - @test_cooks
   - и т.д.

2. Добавьте бота во все каналы

3. Настройте .env:

```env
CHANNEL_VACANCIES_BARMEN=@test_barmen
CHANNEL_VACANCIES_WAITERS=@test_waiters
CHANNEL_VACANCIES_COOKS=@test_cooks
CHANNEL_VACANCIES_BARISTA=@test_barista
CHANNEL_VACANCIES_ADMIN=@test_admin
CHANNEL_VACANCIES_SUPPORT=@test_support
CHANNEL_VACANCIES_GENERAL=@test_general

# Резюме могут идти в те же или отдельные каналы
CHANNEL_RESUMES_BARMEN=@test_barmen
CHANNEL_RESUMES_WAITERS=@test_waiters
# и т.д.
```

4. Создайте вакансии разных категорий и проверьте, что они публикуются в правильные каналы

## Возврат к продакшн каналам

Когда закончите тестирование и будете готовы к продакшн:

1. Откройте `.env`
2. Верните оригинальные каналы из `.env.example`:

```bash
# Скопируйте каналы из .env.example
cp .env.example .env.backup
# Или вручную замените на продакшн каналы
```

3. Убедитесь, что бот добавлен во все продакшн каналы как администратор
4. Перезапустите приложение

```bash
docker-compose down
docker-compose up -d
```

## Проверка Publication записей в базе

Каждая публикация сохраняется в MongoDB. Проверить можно так:

```bash
# Подключитесь к MongoDB
docker exec -it click-mongodb-1 mongosh click_db

# Посмотрите публикации
db.publications.find().pretty()

# Или только последние
db.publications.find().sort({created_at: -1}).limit(5).pretty()

# Найти неудачные публикации
db.publications.find({is_published: false}).pretty()
```

## Быстрый чеклист для тестирования

- [ ] Создал тестовый канал в Telegram
- [ ] Добавил бота как администратора с правами на публикацию
- [ ] Обновил .env с username/chat_id канала
- [ ] Перезапустил приложение (docker-compose down && docker-compose up -d)
- [ ] Создал и опубликовал тестовую вакансию
- [ ] Пост с вакансией появился в канале
- [ ] Создал и опубликовал тестовое резюме
- [ ] Пост с резюме появился в канале
- [ ] Проверил логи - нет ошибок публикации

Готово! Публикация в Telegram каналы работает! 🎉
