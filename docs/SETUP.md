# CLICK - Инструкция по запуску и использованию

## Статус реализации

### ✅ Приоритет 1 (MVP) - ГОТОВО

1. ✅ **Конструктор резюме** - полностью реализован
   - Пошаговый FSM для создания резюме
   - Все поля из спецификации (60+ должностей, навыки, опыт работы, образование)
   - Превью и публикация
   - Автоматическая публикация в Telegram каналы

2. ✅ **API Backend** - полностью функционален
   - 40+ REST endpoints для резюме, вакансий, откликов
   - MongoDB integration с Beanie ODM
   - Полная валидация данных через Pydantic V2

3. ✅ **Telegram Bot** - базовый функционал
   - Регистрация пользователей
   - Выбор роли (соискатель/работодатель)
   - Полный конструктор резюме
   - Просмотр своих резюме и откликов

4. ✅ **Автопубликация в Telegram каналы**
   - TelegramPublisher сервис
   - Интеграция с API endpoints
   - Автоматический выбор канала по категории должности
   - Красивое форматирование постов с кнопками

5. ✅ **Конструктор вакансий** - полностью реализован
   - Пошаговый FSM для создания вакансии
   - Все поля: должность, компания, локация, зарплата, требования
   - Условия работы, бонусы, описание обязанностей
   - Превью и публикация
   - Автоматическая публикация в Telegram каналы
   - Просмотр своих вакансий и откликов

6. ✅ **Поиск вакансий для соискателей** - полностью реализован
   - Поиск по категории должности
   - Поиск по ключевым словам
   - Просмотр всех вакансий
   - Навигация по результатам (предыдущая/следующая)
   - Подробная карточка вакансии
   - Отклик на вакансию с выбором резюме
   - Сопроводительное письмо

7. ✅ **Поиск резюме для работодателей** - полностью реализован
   - Поиск по категории должности
   - Поиск по ключевым словам
   - Просмотр всех резюме
   - Навигация по результатам
   - Подробная карточка резюме с контактами
   - Приглашение кандидата на вакансию
   - Персональное сообщение кандидату

8. ✅ **Система уведомлений в Telegram** - полностью реализована
   - Уведомления работодателям о новых откликах
   - Уведомления соискателям о приглашениях
   - Уведомления об изменении статуса отклика
   - Автоматическая отправка через NotificationService
   - Неблокирующая отправка (фоновые задачи)

9. ✅ **Управление откликами для работодателей** - полностью реализовано
   - Просмотр откликов по вакансиям
   - Навигация между откликами
   - Просмотр полного резюме кандидата
   - Принять отклик (статус: accepted)
   - Отклонить отклик (статус: rejected)
   - Пригласить на собеседование (статус: invited)
   - Автоматические уведомления кандидатам

10. ✅ **Аналитика и статистика** - полностью реализовано
    - Общая статистика пользователя (просмотры, отклики, конверсии)
    - Детальная аналитика по каждому резюме
    - Детальная аналитика по каждой вакансии
    - Трендовые позиции на рынке
    - Разбивка откликов по статусам
    - Расчет конверсий и эффективности

11. ✅ **Рекомендательная система** - полностью реализована
    - Умный подбор вакансий для соискателей на основе резюме
    - Подбор кандидатов для работодателей на основе вакансий
    - Алгоритм matching со scoring (0-100 баллов):
      - Совпадение должности: 30%
      - Совпадение навыков: 25%
      - Совпадение локации: 15%
      - Совместимость зарплаты: 15%
      - Совпадение опыта: 10%
      - Совпадение образования: 5%
    - Детальная информация о каждом совпадении
    - Навигация по рекомендациям
    - Быстрые действия (откликнуться/пригласить)

### 📋 Приоритет 2 - Планируется

- Импорт существующих 5000+ контактов из Google Sheets
- API для менеджеров с токенами
- Web-приложение

## Быстрый старт

### 1. Предварительные требования

Убедитесь, что установлено:
- Docker & Docker Compose
- Git
- Python 3.11+ (для локальной разработки)

### 2. Клонирование и настройка

```bash
cd /home/mixelka/PycharmProjects/click

# Создайте .env файл
cp .env.example .env

# Откройте .env и заполните обязательные параметры
nano .env
```

### 3. Обязательные переменные в .env

```env
# ВАЖНО: Получите токен бота у @BotFather в Telegram
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Любая случайная строка для шифрования (минимум 32 символа)
SECRET_KEY=your_very_long_secret_key_change_this_in_production

# MongoDB (можно оставить по умолчанию для Docker)
MONGODB_URL=mongodb://mongodb:27017
MONGODB_DB_NAME=click_db

# Redis (можно оставить по умолчанию)
REDIS_HOST=redis
REDIS_PORT=6379

# Остальные параметры можно оставить как есть
```

### 4. Настройка Telegram каналов (опционально)

По умолчанию используются продакшн каналы. **Для тестирования** настоятельно рекомендуется создать свои тестовые каналы:

**📚 Дополнительные инструкции:**
- [CHANNELS_QUICK_SETUP.md](CHANNELS_QUICK_SETUP.md) - Быстрая шпаргалка (3 шага)
- [TESTING.md](TESTING.md) - Подробная инструкция по тестированию
- [scripts/README.md](scripts/README.md) - Автоматические скрипты настройки

**🚀 Быстрая настройка через скрипт:**
```bash
./scripts/setup_test_channels.sh @your_test_channel
docker-compose restart
```

#### Создание тестовых каналов:

1. **Создайте канал в Telegram:**
   - Откройте Telegram
   - Создайте новый канал (Public или Private)
   - Если Public - запомните username (например: @my_test_channel)
   - Если Private - получите chat_id через @getmyid_bot

2. **Добавьте бота в канал как администратора:**
   - Откройте настройки канала
   - Administrators → Add Administrator
   - Найдите вашего бота и добавьте с правами на публикацию

3. **Настройте каналы в .env:**

```env
# Для простого тестирования - используйте один тестовый канал для всех категорий
CHANNEL_VACANCIES_BARMEN=@my_test_channel
CHANNEL_VACANCIES_WAITERS=@my_test_channel
CHANNEL_VACANCIES_COOKS=@my_test_channel
CHANNEL_VACANCIES_BARISTA=@my_test_channel
CHANNEL_VACANCIES_ADMIN=@my_test_channel
CHANNEL_VACANCIES_SUPPORT=@my_test_channel
CHANNEL_VACANCIES_OTHER=@my_test_channel
CHANNEL_VACANCIES_GENERAL=@my_test_channel

# Резюме могут публиковаться в тот же тестовый канал
CHANNEL_RESUMES_BARMEN=@my_test_channel
CHANNEL_RESUMES_WAITERS=@my_test_channel
CHANNEL_RESUMES_COOKS=@my_test_channel
CHANNEL_RESUMES_BARISTA=@my_test_channel
CHANNEL_RESUMES_ADMIN=@my_test_channel
CHANNEL_RESUMES_SUPPORT=@my_test_channel
CHANNEL_RESUMES_OTHER=@my_test_channel
CHANNEL_RESUMES_GENERAL=@my_test_channel
```

**Для продакшна (уже настроено по умолчанию):**
- Вакансии: @horeca_msk1, @horeca_msk2, @horeca_povara1, @horeca_barista, @horeca_admin_man, @horeca5, @HoReCaMBA
- Резюме: те же каналы (можно настроить отдельно)

**Важно:**
- Бот должен быть администратором канала с правами на публикацию
- Для private каналов используйте chat_id вместо @username (например: -1001234567890)
- Если публикация не работает - проверьте права бота в канале

### 5. Запуск через Docker Compose

```bash
# Запустить все сервисы
docker-compose up -d

# Проверить статус
docker-compose ps

# Посмотреть логи
docker-compose logs -f bot        # Логи бота
docker-compose logs -f backend    # Логи API
docker-compose logs -f mongodb    # Логи базы данных

# Остановить все сервисы
docker-compose down

# Остановить и удалить все данные
docker-compose down -v
```

### 5. Проверка работы

После запуска будут доступны:

- **Telegram Bot**: Найдите бота по username (указан в @BotFather)
- **API Documentation**: http://localhost:8000/docs
- **API ReDoc**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/api/v1/health
- **Flower (Celery)**: http://localhost:5555

**Проверка API:**
```bash
# Проверка здоровья
curl http://localhost:8000/api/v1/health

# Должен вернуть:
# {"status":"healthy","timestamp":"...","version":"1.0.0","environment":"development","database":"mongodb"}
```

**Проверка бота:**
1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Выберите роль (Я соискатель / Я работодатель)
5. Попробуйте создать резюме

## Локальная разработка

### Установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### Запуск компонентов отдельно

```bash
# 1. Запустите только базу данных через Docker
docker-compose up mongodb redis -d

# 2. В первом терминале - Backend API
source venv/bin/activate
python -m backend.main

# 3. Во втором терминале - Telegram Bot
source venv/bin/activate
python -m bot.main

# 4. (Опционально) В третьем терминале - Celery Worker
source venv/bin/activate
celery -A backend.celery_app worker --loglevel=info
```

### Полезные команды Make

```bash
# Показать все команды
make help

# Установить зависимости
make install

# Запустить в режиме разработки
make dev

# Запустить тесты
make test

# Проверить код (линтинг)
make lint

# Форматировать код
make format

# Docker команды
make docker-up        # Запустить Docker
make docker-down      # Остановить Docker
make docker-logs      # Показать логи
make docker-rebuild   # Пересобрать контейнеры
```

## Использование бота

### Для соискателей

1. **Регистрация:**
   - Отправьте `/start`
   - Выберите "Я соискатель"

2. **Создание резюме:**
   - Нажмите "📝 Создать резюме"
   - Следуйте пошаговым инструкциям:
     - ФИО
     - Город
     - Готовность к переезду/командировкам
     - Телефон и email
     - Желаемая должность (выбор из категорий)
     - Зарплата и график работы
     - Опыт работы (можно добавить несколько)
     - Образование
     - Навыки (выбор из списка + свои)
     - О себе
   - Просмотрите превью
   - Нажмите "Опубликовать"

3. **Просмотр резюме:**
   - Нажмите "📋 Мои резюме"
   - Увидите список с просмотрами и откликами

4. **Отклики:**
   - Нажмите "📬 Мои отклики"
   - Увидите статусы всех откликов

5. **Поиск вакансий:**
   - Нажмите "🔍 Найти вакансию"
   - Выберите способ поиска:
     - По категории должности
     - Поиск по ключевым словам
     - Просмотр всех вакансий
   - Листайте результаты кнопками ⬅️ Пред. / ➡️ След.
   - Нажмите "📋 Подробнее" для полной информации
   - Нажмите "✉️ Откликнуться" чтобы отправить отклик:
     - Выберите резюме
     - Напишите сопроводительное письмо (опционально)
     - Подтвердите отправку

6. **Рекомендации вакансий:**
   - Нажмите "💡 Рекомендации"
   - Если у вас несколько резюме - выберите нужное
   - Увидите подборку вакансий с процентом совпадения
   - Для каждой рекомендации показаны:
     - Процент совпадения (0-100%)
     - Детали совпадения (должность, навыки, локация, зарплата)
     - Полная информация о вакансии
   - Листайте рекомендации кнопками ◀️ / ▶️
   - Действия:
     - 👀 Полное описание - посмотреть всю информацию
     - ✅ Откликнуться - сразу отправить отклик

7. **Моя статистика:**
   - Нажмите "📊 Моя статистика"
   - Увидите общую статистику:
     - Количество резюме
     - Общее количество просмотров
     - Среднее просмотров на резюме
     - Отправленные заявки и полученные приглашения
     - Процент успеха (принятые отклики)
   - Детальная статистика по каждому резюме доступна отдельно

### Для работодателей

1. **Регистрация:**
   - Отправьте `/start`
   - Выберите "Я работодатель"

2. **Создание вакансии:**
   - Нажмите "📝 Создать вакансию"
   - Следуйте пошаговым инструкциям:
     - Должность и категория
     - Информация о компании (название, тип, описание, размер)
     - Местоположение (город, адрес, метро)
     - Контактное лицо
     - Зарплата и условия оплаты
     - Тип занятости и график работы
     - Требования (опыт, образование, навыки)
     - Условия (трудовой договор, испытательный срок, удаленка)
     - Бонусы и требуемые документы
     - Описание и обязанности
     - Настройки публикации
   - Просмотрите превью
   - Нажмите "Опубликовать"

3. **Управление вакансиями:**
   - Нажмите "📋 Мои вакансии"
   - Увидите список с просмотрами и откликами

4. **Управление откликами:**
   - Нажмите "📬 Управление откликами"
   - Выберите вакансию из списка
   - Просматривайте отклики кнопками ⬅️ Пред. / ➡️ След.
   - Доступные действия:
     - 📋 Полное резюме - посмотреть всю информацию о кандидате
     - ✅ Принять - принять кандидата (статус: принят)
     - ❌ Отклонить - отклонить кандидата
     - ✅ Пригласить - пригласить на собеседование (для просмотренных)
   - Кандидат автоматически получит уведомление о решении

5. **Поиск резюме:**
   - Нажмите "🔍 Найти резюме"
   - Выберите способ поиска:
     - По категории должности
     - Поиск по ключевым словам
     - Просмотр всех резюме
   - Листайте результаты кнопками ⬅️ Пред. / ➡️ След.
   - Нажмите "📋 Подробнее" для полной информации с контактами
   - Нажмите "✉️ Пригласить" чтобы пригласить кандидата:
     - Выберите вакансию
     - Напишите приглашение кандидату
     - Подтвердите отправку

6. **Рекомендованные кандидаты:**
   - Нажмите "💡 Рекомендованные кандидаты"
   - Если у вас несколько вакансий - выберите нужную
   - Увидите подборку кандидатов с процентом совпадения
   - Для каждой рекомендации показаны:
     - Процент совпадения (0-100%)
     - Детали совпадения (должность, навыки, локация, зарплата, опыт)
     - Краткая информация о кандидате
   - Листайте рекомендации кнопками ◀️ / ▶️
   - Действия:
     - 👀 Полное резюме - посмотреть всю информацию с контактами
     - 📧 Пригласить - сразу отправить приглашение

7. **Моя статистика:**
   - Нажмите "📊 Моя статистика"
   - Увидите общую статистику:
     - Количество вакансий (всего, опубликовано, активных)
     - Общее количество просмотров
     - Среднее просмотров на вакансию
     - Количество откликов и среднее на вакансию
     - Конверсия просмотры→отклики
     - Процент принятия кандидатов
   - Детальная статистика по каждой вакансии доступна отдельно

## Структура API

### Users

- `POST /api/v1/users` - Создать пользователя
- `GET /api/v1/users/{id}` - Получить пользователя
- `GET /api/v1/users/telegram/{telegram_id}` - По Telegram ID
- `GET /api/v1/users` - Список с фильтрами
- `PATCH /api/v1/users/{id}` - Обновить
- `DELETE /api/v1/users/{id}` - Удалить

### Resumes

- `POST /api/v1/resumes` - Создать резюме
- `GET /api/v1/resumes/{id}` - Получить резюме
- `GET /api/v1/resumes` - Список с фильтрами
- `GET /api/v1/resumes/user/{user_id}` - Резюме пользователя
- `GET /api/v1/resumes/search?q=...` - Поиск
- `PATCH /api/v1/resumes/{id}` - Обновить
- `PATCH /api/v1/resumes/{id}/publish` - Опубликовать
- `PATCH /api/v1/resumes/{id}/archive` - Архивировать
- `DELETE /api/v1/resumes/{id}` - Удалить

### Vacancies

- `POST /api/v1/vacancies` - Создать вакансию
- `GET /api/v1/vacancies/{id}` - Получить вакансию
- `GET /api/v1/vacancies` - Список с фильтрами
- `GET /api/v1/vacancies/user/{user_id}` - Вакансии работодателя
- `GET /api/v1/vacancies/search?q=...` - Поиск
- `GET /api/v1/vacancies/{id}/analytics` - Аналитика
- `PATCH /api/v1/vacancies/{id}` - Обновить
- `PATCH /api/v1/vacancies/{id}/publish` - Опубликовать
- `PATCH /api/v1/vacancies/{id}/pause` - Приостановить
- `PATCH /api/v1/vacancies/{id}/archive` - Архивировать
- `DELETE /api/v1/vacancies/{id}` - Удалить

### Responses (Отклики)

- `POST /api/v1/responses` - Создать отклик
- `POST /api/v1/responses/invitation` - Пригласить кандидата
- `GET /api/v1/responses/{id}` - Получить отклик
- `GET /api/v1/responses/vacancy/{vacancy_id}` - Отклики на вакансию
- `GET /api/v1/responses/applicant/{applicant_id}` - Отклики соискателя
- `GET /api/v1/responses/employer/{employer_id}` - Все отклики работодателя
- `PATCH /api/v1/responses/{id}/status` - Обновить статус
- `DELETE /api/v1/responses/{id}` - Удалить

### Analytics (Аналитика)

- `GET /api/v1/analytics/my-statistics` - Общая статистика пользователя
- `GET /api/v1/analytics/vacancy/{vacancy_id}` - Детальная аналитика вакансии
- `GET /api/v1/analytics/resume/{resume_id}` - Детальная аналитика резюме
- `GET /api/v1/analytics/trending-positions?limit=10` - Трендовые позиции

**Примеры аналитики:**

Аналитика вакансии включает:
- Дни активности
- Количество просмотров и откликов
- Конверсия просмотры→отклики
- Разбивка откликов по статусам
- Среднее время до первого отклика

Аналитика резюме включает:
- Дни активности
- Количество просмотров
- Отправленные заявки vs полученные приглашения
- Процент успеха (принятые отклики)
- Разбивка по статусам

### Recommendations (Рекомендации)

- `GET /api/v1/recommendations/vacancies-for-resume/{resume_id}?limit=10&min_score=40` - Рекомендованные вакансии
- `GET /api/v1/recommendations/resumes-for-vacancy/{vacancy_id}?limit=10&min_score=40` - Рекомендованные кандидаты
- `GET /api/v1/recommendations/match-score/{resume_id}/{vacancy_id}` - Расчет совпадения

**Параметры:**
- `limit` (1-50): Количество рекомендаций
- `min_score` (0-100): Минимальный процент совпадения

**Ответ включает:**
- Объект вакансии/резюме
- Процент совпадения (score)
- Детали совпадения (match_details):
  - Совпадение должности
  - Совпадение локации
  - Совместимость зарплаты
  - Совпавшие навыки
  - Достаточность опыта

**Алгоритм scoring:**
- Должность: 30 баллов (15 за частичное совпадение)
- Навыки: 25 баллов (пропорционально количеству совпадений)
- Локация: 15 баллов (10 если готов к переезду)
- Зарплата: 15 баллов (10 если в пределах ±10-20%)
- Опыт: 10 баллов (градация 0.25/0.5/0.75/1.0)
- Образование: 5 баллов

## Уведомления

Платформа автоматически отправляет уведомления в Telegram:

### Для соискателей:
- ✅ Резюме успешно опубликовано
- 🔔 Новое приглашение от работодателя
- 👀 Ваш отклик просмотрен
- ✅ Вас приглашают на собеседование
- 🎉 Ваш отклик принят
- ❌ Отклик отклонен

### Для работодателей:
- ✅ Вакансия успешно опубликована
- 🔔 Новый отклик на вашу вакансию
- 📋 Информация о кандидате

Все уведомления приходят мгновенно и содержат ключевую информацию!

## Telegram каналы

При публикации резюме/вакансии автоматически создаётся пост в соответствующем канале:

### Вакансии:
- @horeca_msk1 - Бармены
- @horeca_msk2 - Официанты
- @horeca_povara1 - Повара
- @horeca_barista - Баристы
- @horeca_admin_man - Управление и администрирование
- @horeca5 - Вспомогательный персонал
- @HoReCaMBA - Общий канал

### Резюме:
(Используются те же каналы, планируется создать отдельные для работодателей)

## Troubleshooting

### Бот не отвечает

1. Проверьте, что бот запущен:
   ```bash
   docker-compose ps
   # bot должен быть в состоянии "Up"
   ```

2. Проверьте логи:
   ```bash
   docker-compose logs bot | tail -50
   ```

3. Проверьте BOT_TOKEN в `.env`

### API возвращает ошибки

1. Проверьте, что MongoDB запущен:
   ```bash
   docker-compose ps mongodb
   ```

2. Проверьте подключение:
   ```bash
   curl http://localhost:8000/api/v1/health/detailed
   ```

3. Проверьте логи backend:
   ```bash
   docker-compose logs backend | tail -50
   ```

### Резюме не публикуется в каналы

1. Проверьте, что бот добавлен в каналы как администратор
2. Проверьте логи на ошибки публикации:
   ```bash
   docker-compose logs backend | grep "publish"
   ```

3. Убедитесь, что каналы указаны правильно в `.env`

## Следующие шаги

1. ✅ Конструктор резюме - ГОТОВ
2. ✅ Конструктор вакансий - ГОТОВ
3. ✅ Поиск вакансий для соискателей - ГОТОВ
4. ✅ Поиск резюме для работодателей - ГОТОВ
5. ✅ Отклики на вакансии - ГОТОВ
6. ✅ Приглашения кандидатов - ГОТОВ
7. ✅ Система уведомлений в Telegram - ГОТОВО
8. ✅ Управление откликами через бота - ГОТОВО
9. 📋 Аналитика и статистика
10. 📋 Рекомендательная система
11. 📋 Импорт контактов из Google Sheets (опционально)
12. 📋 Web-приложение (опционально)

## Контакты

- **Техподдержка**: @click_support
- **Документация**: http://localhost:8000/docs
- **GitHub Issues**: (ссылка на репозиторий)
